const UserSettings = () => {
  return (
    <div className="mt-20 p-10 flex flex-col gap-10">
      <p>User Settings Page</p>
      <p>Only logged user should see this page.</p>
    </div>
  );
};

export default UserSettings;
